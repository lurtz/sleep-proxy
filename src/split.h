#pragma once

#include <vector>
#include <string>

/**
 * split a string into multiple pieces at each occuring position of delin
 */
std::vector<std::string> split(const std::string& string, const char delim);

